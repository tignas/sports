{% extends "templates/league/base.jinja2" %}

{% block league_content %}

<ul id="view_types">
{% for view_type in view_types %}
    <li><a href="{{view_type}}/">{{view_type}}</a></li>
{% endfor %}
</ul>

{% for conference, divisions in teams.items() %}
<h2>{{conference}}</h2>
    <table>
        {% for division, teams in divisions.items() %}
        <thead>
            <tr>
                <th>{{division}}</th>
                <th>Home</th>
                <th>Away</th>
                <th>Overall</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><a href="{{team['Team'].url()}}">{{team['Team'].name}}</a></td>
                <td>{{team['home_wins']}}-{{team['home_loss']}}</td>
                <td>{{team['away_wins']}}-{{team['away_loss']}}</td>
            </tr>
            {% endfor %}
        </tbody>
        {% endfor %}
    </table>
{% endfor %}
{% endblock %}

