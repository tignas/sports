<br />
<div id="roster">
{% for pos, value in roster.items() %}
{% if pos == 'flex' %}
    <label for="{{pos}}">{{pos}}</label>
    <label for="flex_positions">Positions</label>
    {% for pos in value['positions'] %}
    <input type="checkbox" name="flex_positions" value="{{pos}}">{{pos}}
    {% endfor %}
    <input id="flex" name="flex" value="{{value['number']}}" type="number"/>
{% else %}
<label for="{{pos}}">{{pos}}</label>
<input id="{{pos}}" name="{{pos}}" value="{{value}}" type="number"/>
{% endif %}
{% endfor %}
</div>
