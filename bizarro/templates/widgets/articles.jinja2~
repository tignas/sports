<div id="articles">
    <h2>Article</h2>
    <ul>
        {% for article in articles %}
        <li><a href="{{article.url}}">{{article.title}}</a> - <a href="{{article.source.url}}">{{article.source.name}}</a></li>
        {% endfor %}
    </ul>
</div>
