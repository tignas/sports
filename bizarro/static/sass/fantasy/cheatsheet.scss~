@import "colors";
@import "fonts";

#content {
    width: 11in;
}

.player_table {
    float: right;
    width: 3.33in;
    margin: .33in;
}
.player_table:nth-child(n+1) {
    float: left;
}
.player_table:nth-child(n+2) {
    float: left;
}
caption {
    text-align: left;
    color: $green_2;
    font-family: $sans_serif;
    font-weight: bold;
    margin-bottom: 5px;
    margin-left: 15px;
}
